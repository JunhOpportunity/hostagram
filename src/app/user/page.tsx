import UserPosts from "../components/UserPost";
import UserProfile from "../components/UserProfile";

export default function UserPage() {
  return (
    <>
      {/*<UserProfile currentUser={currentUser} />*/}
      {/*<UserPosts postList={posts} />*/}
    </>
  );
}
